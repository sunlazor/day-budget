{% extends 'base.html.twig' %}

{% block body %}
    <h1>Budget</h1>

    {% for line in budgetItems %}

        <div>{{ line.title }}</div>
        <div>{{ line.amount }}</div>
    {% endfor %}


{#    <twig:Budget lineItems="{{budgetItems}}"/>#}
{% endblock %}